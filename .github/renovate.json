{
    "extends": ["config:base"],
    "packageRules": [
      {
        "description": "Update dependencies only in specific paths",
        "fileMatch": [
            "^maybe/.*"
        ],
        "matchPackagePatterns": ["*"],
        "enabled": true
      },
      {
        "description": "Disable updates for all other paths",
        "matchPackagePatterns": ["*"],
        "enabled": false
      }
    ]
  }